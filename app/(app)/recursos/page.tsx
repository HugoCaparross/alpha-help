import MaterialesView from "@/components/resources/MaterialesView";

/**
 * Página principal de los materiales
 * de apoyo del programa.
 *
 * Toda la lógica de negocio y presentación
 * se encuentra encapsulada en MaterialesView.
 */
export default function MaterialesPage() {
  return <MaterialesView />;
}